# The URL the site will be built for
base_url = "https://irlug.it/"

title = "IrLUG"
description = "IrLUG è un gruppo di appassionati senza scopo di lucro nato per diffondere la cultura del free software e dell'open source in Irpinia e dintorni"

theme = "terminus"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", a feed is automatically generated.
generate_feeds = true

# The default author for pages
author = "IrLUG"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true


[markdown]

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether footnotes are rendered in the GitHub-style (at the bottom, with back references) or plain (in the place, where they are defined)
bottom_footnotes = true

# CSS class to add to external links, so the theme can mark them with an
# outbound-link icon.
external_links_class = "external"

# Whether to set decoding="async" and loading="lazy" for all images.
# When turned on, the alt text must be plain text.
lazy_async_image = true

# When set to "true", support for GitHub-style alerts, a.k.a. callouts or
# admonitions, is enabled in the Markdown parser.
github_alerts = true

[markdown.highlighting]
# Whether to use inline hex colours ("inline") or CSS classes ("class").
# See https://www.getzola.org/documentation/content/syntax-highlighting/ for details.
style = "inline"

# Eyal: Use a slightly patched version of the Monokai theme that plays more nicely with Rust.
# Long term solution is to improve the Rust grammar used by VSCode/Giallo, which is lacking:
# https://github.com/dustypomerleau/rust-syntax
extra_themes = ["themes/terminus/syntaxes/monokai-classic.json"]

# The theme to use for code highlighting.
# See https://github.com/getzola/giallo?tab=readme-ov-file#themes for the list of allowed values.
theme = "monokai-classic"

[extra]
color_scheme = "irpinia"
# Keeps the switcher in the navbar; the theme gates it behind this flag,
# which defaults to false.
color_scheme_switcher = true
header_logo = "img/logo-big.png"
close_responsive_menu_on_resize = true
copy_button = true
favicon = "img/logo-big.png"
layout = "center"
show_default_author = false

main_menu = [
    { name = "blog", url = "blog", trailing_slash = true },
    { name = "eventi", url = "eventi", trailing_slash = true },
    { name = "privacy", url = "privacy", trailing_slash = true },
    { name = "old", url = "/old", new_tab = true },
]

socials = [
    { name = "email", url = "mailto:info@irlug.it" },
    { name = "github", url = "https://github.com/irlug" },
    { name = "youtube", url = "https://www.youtube.com/channel/UCg0SEu8SEFB9bh72WfYL_Vg" },
    { name = "instagram", url = "https://www.instagram.com/irpinialug/" },
    { name = "telegram", url = "https://t.me/irlug" },
    { name = "facebook", url = "https://www.facebook.com/IrLUG" },
    { name = "flickr", url = "https://www.flickr.com/photos/140926019@N05/albums" },
]

stylesheets = [
    "css/logo.css"
]

[extra.content_security_policy]
enable = true

allowed_domains = [
    # Le icone dei link esterni e dei blocchi NOTE/TIP sono `data:` URI usate
    # come mask nel CSS: senza questa voce la CSP le blocca. Il tema la
    # aggiungeva da solo finche' la favicon era un'emoji.
    { directive = "img-src", domains = ["'self'", "data:"] },
    # Gli iframe incorporati nei post vengono bloccati
    # se non si elencano qui le origini dei frame.
    { directive = "frame-src", domains = ["https://www.youtube.com", "https://www.facebook.com"] },
]

[extra.responsive_images]
widths = [640, 784, 1280, 1920, 2560]
fallback_width = 1280
